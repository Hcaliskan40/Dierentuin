@model Dierentuin.Models.Enclosure

    <h2>Delete Enclosure</h2>
    <p>Are you sure you want to delete this enclosure?</p>

    <div class="form-group">
    <dl class="row">
    <dt class="col-sm-2">Name</dt>
    <dd class="col-sm-10">@Model.Name</dd>
    <dt class="col-sm-2">Size</dt>
    <dd class="col-sm-10">@Model.Size m²</dd>
    <dt class="col-sm-2">Climate</dt>
    <dd class="col-sm-10">@Model.Climate</dd>
    <dt class="col-sm-2">Habitat Type</dt>
    <dd class="col-sm-10">@Model.HabitatType</dd>
    <dt class="col-sm-2">Security Level</dt>
    <dd class="col-sm-10">@Model.SecurityLevel</dd>
    </dl>
    </div>

    <form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </form>